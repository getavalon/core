{
    "$schema": "http://json-schema.org/schema#",

    "title": "mindbender-core:asset-2.0",
    "description": "A unit of data",

    "type": "object",

    "additionalProperties": true,

    "required": [
        "schema",
        "type",
        "type",
        "name",
        "silo",
        "data"
    ],

    "properties": {
        "schema": {
            "description": "Schema identifier for payload",
            "type": "string",
            "const": "mindbender-core:asset-2.0",
            "example": "mindbender-core:asset-2.0"
        },
        "type": {
            "description": "The type of document",
            "type": "string",
            "const": "asset",
            "example": "asset"
        },
        "name": {
            "description": "Name of asset",
            "type": "string",
            "pattern": "^[a-zA-Z0-9_.]*$",
            "example": "Bruce"
        },
        "silo": {
            "description": "Group or container of asset",
            "type": "string",
            "example": "assets"
        },
        "data": {
            "description": "Document metadata",
            "type": "object",
            "example": {"key": "value"}
        }
    },

    "definitions": {}
}