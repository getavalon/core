{
    "$schema": "http://json-schema.org/schema#",

    "title": "pyblish-starter:container-1.0",
    "description": "A loaded asset",

    "type": "object",

    "additionalProperties": true,

    "required": [
        "schema",
        "path"
    ],

    "properties": {
        "schema": {"type": "string"},
        "path": {
            "description": "Absolute path",
            "type": "string"
        }
    }
}
