{
    "$schema": "http://json-schema.org/schema#",

    "title": "mindbender-core:subset-2.0",
    "description": "A container of instances",

    "type": "object",

    "additionalProperties": true,

    "required": [
        "schema",
        "type",
        "name",
        "data"
    ],

    "properties": {
        "schema": {
            "description": "The schema associated with this document",
            "type": "string",
            "const": "mindbender-core:subset-2.0",
            "example": "mindbender-core:subset-2.0"
        },
        "type": {
            "description": "The type of document",
            "type": "string",
            "const": "subset",
            "example": "subset"
        },
        "name": {
            "description": "Name of directory",
            "type": "string",
            "pattern": "^[a-zA-Z0-9_.]*$",
            "example": "shot01"
        },
        "data": {
            "type": "object",
            "description": "Document metadata",
            "example": {
                "startFrame": 1000,
                "endFrame": 1201
            }
        }
    }
}